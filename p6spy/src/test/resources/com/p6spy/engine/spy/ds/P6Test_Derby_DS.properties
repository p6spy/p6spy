realdatasource=jdbc/a
realdatasourceclass=org.apache.derby.jdbc.EmbeddedDataSource

# TODO: whenever we'll support multiple XA DataSources we should enable this
#realdatasource2=jdbc/b
#realdatasourceclass2=org.apache.derby.jdbc.EmbeddedXADataSource

# this section is present just to make JUnit tests happy, so that we have all the drivers re-registered prio to test run
# as we unregister all of them prio to each run
# would not need it in real world
p6realdriver=org.apache.derby.jdbc.EmbeddedDriver
p6driver=com.p6spy.engine.spy.P6SpyDriver